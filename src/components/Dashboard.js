/*import React from "react";
import { useMqtt } from "../hooks/useMqtt";

export default function Dashboard() {
  const { voltage, current, power, total } = useMqtt();

  return (
    <div>
      <h1>⚡ Energy Dashboard</h1>
      <p>Voltage: {voltage ?? "—"} V</p>
      <p>Current: {current ?? "—"} A</p>
      <p>Power: {power ?? "—"} W</p>
      <p>Total: {total ?? "—"} kWh</p>
    </div>
  );
}*/
